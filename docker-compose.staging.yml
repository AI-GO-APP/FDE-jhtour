services:
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - target: 3000
        published: ${PORT:-3001}
        protocol: tcp
        mode: host
    restart: always
