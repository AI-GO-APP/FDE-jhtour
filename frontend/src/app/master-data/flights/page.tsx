'use client';
/** A20. 航班時刻表 | L_flt.asp | DB: flights */
import React from 'react'; import { Form, Input } from 'antd'; import type { ColumnsType } from 'antd/es/table'; import PageShell from '@/components/page-shell/PageShell'; import { mockFlights } from '@/mock/master-data'; import type { Flight } from '@/types';
const columns: ColumnsType<Flight> = [{ title: '航班號', dataIndex: 'FLT_NO', width: 100 },{ title: '航空', dataIndex: 'CARR_CD', width: 80 },{ title: '出發', dataIndex: 'DEP_AIRP', width: 80 },{ title: '到達', dataIndex: 'ARR_AIRP', width: 80 },{ title: '出發時間', dataIndex: 'DEP_TIME', width: 100 },{ title: '到達時間', dataIndex: 'ARR_TIME', width: 100 },{ title: '營運日', dataIndex: 'FLT_DAY', width: 80 }];
const formContent = (<>{/* === [API] POST/PUT /api/flights === DB: INSERT/UPDATE flights === */}<Form.Item name="FLT_NO" label="航班號" rules={[{ required: true }]}><Input /></Form.Item><Form.Item name="CARR_CD" label="航空公司"><Input /></Form.Item><Form.Item name="DEP_AIRP" label="出發機場"><Input /></Form.Item><Form.Item name="ARR_AIRP" label="到達機場"><Input /></Form.Item><Form.Item name="DEP_TIME" label="出發時間"><Input placeholder="HH:MM" /></Form.Item><Form.Item name="ARR_TIME" label="到達時間"><Input placeholder="HH:MM" /></Form.Item></>);
export default function Page() { return <PageShell title="航班時刻表" columns={columns} dataSource={mockFlights as unknown as Flight[]} rowKey="FLT_NO" formContent={formContent} searchPlaceholder="搜尋航班號或航空公司..." />; }
